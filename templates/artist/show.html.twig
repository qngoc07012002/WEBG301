{% extends 'base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
    <h1>Artist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artist.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ artist.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a style="width:100px;" class="button-58" href="{{ path('app_artist_index') }}">back to list</a>

    <a style="width:100px; position: relative; top:10px; " class="button-58" href="{{ path('app_artist_edit', {'id': artist.id}) }}">edit</a>

    
{% endblock %}
